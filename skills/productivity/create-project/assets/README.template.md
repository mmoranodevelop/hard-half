# [Project name]

<!--
  The cold start for a human. A reader who cannot tell what this does within
  ten seconds leaves — so lead with what it DOES, not with how to install it.

  Every command here must work today. A README describing a build that does
  not exist yet trains people to distrust the repository.
-->

[One sentence: what this does. Plain language, no jargon, no marketing.]

[Optional second paragraph: why it exists, or what problem it solves. Only if
it is not obvious from the first sentence.]

## Quickstart

<!-- Working, copyable, in order. If it takes more than a minute to get
     something running, say what the long step is and why. -->

```bash
[install]
```

```bash
[run]
```

[What the reader should see when it works.]

## Usage

[The two or three things people actually do with this. Real examples with
real output beat an exhaustive option list — that belongs in `--help` or
`docs/`.]

## How it works

<!-- Optional. A short orientation for someone about to read the code:
     where the entry point is, what the main pieces are, how they connect.
     Two or three sentences, not an architecture document. -->

## Scope

<!-- Optional but valuable, especially early. What this deliberately does NOT
     do. It sets expectations, and it is the strongest defence against scope
     creep that exists. -->

This does not [the deliberate non-goal from the interview], because [reason].

## Development

See [AGENTS.md](./AGENTS.md) for structure, commands, and conventions, and
[STATUS.md](./STATUS.md) for what is done and what is next.

## License

[LICENCE] — see [LICENSE](./LICENSE).
